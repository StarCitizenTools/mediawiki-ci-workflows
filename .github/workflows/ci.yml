name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: reviewdog/action-actionlint@v1
